# Tailscale Configuration
# =======================

# Tailscale Authentication Key
# Generate from: https://login.tailscale.com/admin/settings/keys
# Recommended settings:
#   - Reusable: Yes (for server deployments)
#   - Ephemeral: No (device should persist)
#   - Tags: tag:snowflake-dashboard
#   - Expiration: Set appropriate time or never
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxx

# Additional Tailscale Arguments (optional)
# Examples:
#   --advertise-tags=tag:snowflake-dashboard  (for ACL-based access control)
#   --hostname=snowflake-prod                 (custom hostname, default: snowflake-dashboard)
#   --advertise-routes=10.0.0.0/24            (subnet routing if needed)
TS_EXTRA_ARGS=--advertise-tags=tag:snowflake-dashboard

# Tailscale Certificate Domain (auto-populated by MagicDNS)
# Format: <hostname>.<tailnet-name>.ts.net
# Example: snowflake-dashboard.tail12345.ts.net
# You can find your tailnet name in the Tailscale admin console
TS_CERT_DOMAIN=snowflake-dashboard.tail12345.ts.net

# Your Tailnet Name (optional, for documentation)
# Found in Tailscale admin console URL or device list
TAILNET_NAME=tail12345

# Snowflake Configuration
# ========================
# Same as .env file - see .env.example for details

SNOWFLAKE_ACCOUNT=your-account.region
SNOWFLAKE_USER=your-username
SNOWFLAKE_PASSWORD=your-password
SNOWFLAKE_DATABASE=SNOWFLAKE
SNOWFLAKE_SCHEMA=ACCOUNT_USAGE
SNOWFLAKE_WAREHOUSE=your-warehouse
SNOWFLAKE_ROLE=ACCOUNTADMIN

# Application Port (optional, defaults to 8080)
# This is the internal port, Tailscale will expose on 443
PORT=8080
